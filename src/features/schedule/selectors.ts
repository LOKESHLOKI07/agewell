/**
 * Schedule helpers — period grouping lives in homeViewModel to reuse Today Care items.
 */
export { groupCareItemsByPeriod, schedulePeriodForTime } from '@/features/home/selectors/homeViewModel';
