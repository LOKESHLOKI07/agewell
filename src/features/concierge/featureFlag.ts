/**
 * AgeWell Bot UI gate.
 * Set to `true` in a later release to show Ask AgeWell / chat entry points again.
 * Backend + `/concierge` screen stay in the codebase either way.
 */
export const CONCIERGE_UI_ENABLED = false;
